{
  "name": "aqp-subgraph",
  "version": "1.0.0",
  "license": "GPL-3.0",
  "description": "Subgraph for AQP",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ aqp-subgraph",
    "deploy:dev": "graph deploy --node http://localhost:8020 --ipfs http://localhost:5001 aqp-subgraph",
    "deploy:aws": "graph deploy --node https://starcoin-dev.mercury.eco/graph/ --ipfs https://starcoin-dev.mercury.eco/graph/ipfs/ aqp-subgraph",
    "create-aws": "graph create --node https://starcoin-dev.mercury.eco/graph/ aqp-subgraph",
    "remove-aws": "graph remove --node https://starcoin-dev.mercury.eco/graph/ aqp-subgraph",
    "create-local": "graph create --node http://localhost:8020/ aqp-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ aqp-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 aqp-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.97.1",
    "@graphprotocol/graph-ts": "0.37.0"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
